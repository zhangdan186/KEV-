# 冻结清单

## 1. 冻结范围

本次冻结覆盖：

1. 原始数据字段及语义；
2. 公共衍生字段；
3. 核心函数签名、输入输出和异常语义；
4. 必做80分、GUI 14分、机器学习6分所需输出；
5. 文件命名、表结构、排序、精度和编码；
6. 测试门槛、质量门槛和交付目录；
7. 三人模块边界及合并顺序；
8. 报告和答辩PPT的可追溯关系。

## 2. 冻结版本语义

采用语义化版本：`MAJOR.MINOR.PATCH`。

- `MAJOR`：删除或改名字段、修改函数签名、改变比例分母、改变排序规则、改变输出列等破坏性修改；
- `MINOR`：新增不影响既有调用者的可选字段、函数或输出；
- `PATCH`：文字修订、注释、测试补充或不改变契约的缺陷修复。

## 3. 修改审批

- 破坏性修改：三名成员一致同意，并新增ADR；
- 向后兼容新增：至少两名成员同意，负责人复核；
- 文字与测试修订：一人提交、一人复核。

任何契约修改必须同时更新：

- 对应Markdown文档；
- `src/kev_analysis/constants.py` 或函数签名；
- `contracts/output_registry.yaml`；
- 对应契约测试；
- `CHANGELOG.md`；
- `FREEZE_LOCK.json`。

## 4. 不得擅自修改的内容

- 原始11字段名称；
- `vendor_clean`、`product_clean`、`added_year`、`added_month`、`deadline_days`、`has_cwe`、`cwe_count`；
- `filter_kev(...)` 的指定签名；
- 比例字段使用小数而非百分数文本；
- HHI使用小数份额计算且不乘10000；
- CWE总体、Known、Unknown三个分母；
- GUI直接读取原始JSON；
- 结果排序规则；
- `Unknown` 的解释；
- 原始数据不可被覆盖。

## 5. 当前冻结结论

本包中的文档、代码骨架和机器可读契约已经相互校验。`scripts/verify_freeze.py` 用于检测冻结文件被意外修改。
